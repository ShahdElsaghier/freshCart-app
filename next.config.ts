/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  images: {
    domains: ["ecommerce.routemisr.com"], // add your API host here
  },
};

module.exports = nextConfig;